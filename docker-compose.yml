version: '2'
services:
  app:
    build: .
    container_name: kristiankaa.site
    ports:
      - "8091:80"
    volumes:
      - ./site:/var/www/html
      - ./config/php.development.ini:/etc/php.ini

  db:
    image: mariadb:10.0.22
    container_name: kristiankaa.db
    environment:
      MYSQL_DATABASE: "db_default"
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_USER: "user"
      MYSQL_PASSWORD: "password"
